<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ro.scoalainformala.projectattempt1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectAttempt1">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".DecideActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".WorkoutActivity" />
        <activity android:name=".ExploreWorkouts" />
        <activity android:name=".FragmentProfile" />
        <activity android:name=".FragmentGoals" />
        <activity android:name=".FragmentMain" />
        <activity android:name=".NewFoodItemActivity" />
        <activity android:name=".NewWorkoutItemActivity" />
        <activity android:name=".BuildPlanActivity" />
        <activity android:name=".WeightGoalActivity" />
        <activity android:name=".NutritionGoalActivity"></activity>
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>